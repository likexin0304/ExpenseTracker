# 代码审查问题清单

## 🔴 严重问题

### 问题1: 高置信度时没有自动创建支出
**位置**: `AutoRecognitionViewModel.swift:482`
**问题**: `handleAutoExpenseSuccess`只是保存结果，不会自动创建支出记录
**影响**: 高置信度时应该自动创建，但现在只是等待用户确认
**修复**: 高置信度时应该调用`confirmAndCreateExpense()`自动创建

### 问题2: 创建新实例获取设置
**位置**: `AutoRecognitionViewModel.swift:488`
**问题**: `requiresUserConfirmation`中创建新的`AutoOCRViewModel()`实例来获取设置
**影响**: 每次调用都创建新实例，无法获取用户保存的设置
**修复**: 应该从UserDefaults读取保存的设置

### 问题3: cancellables在async函数中的使用
**位置**: `AutoRecognitionViewModel.swift:398`
**问题**: 在async函数中使用Combine的sink，cancellables可能无法正确存储
**影响**: 可能导致内存泄漏或订阅丢失
**修复**: 确保cancellables正确存储，或使用async/await替代Combine

## 🟡 中等问题

### 问题4: 缺少recordId
**位置**: `AutoRecognitionViewModel.swift:636`
**问题**: `confirmAndCreateExpense`中使用硬编码的"auto_generated_id"
**影响**: 无法正确关联OCR记录
**修复**: 从OCR响应中获取真实的recordId

### 问题5: 线程安全问题
**位置**: `processRecognitionResult`
**问题**: 直接更新@Published属性，可能不在主线程
**影响**: 可能导致UI更新错误
**修复**: 确保所有@Published属性更新都在主线程

## 🟢 轻微问题

### 问题6: 错误处理不完整
**位置**: `performRealRecognition`
**问题**: 某些错误路径可能没有正确处理
**影响**: 用户体验不佳
**修复**: 完善错误处理

