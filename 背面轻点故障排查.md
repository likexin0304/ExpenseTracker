# 背面轻点故障排查指南

## 问题描述

- ✅ 快捷指令单独运行正常
- ❌ 背面轻点三次无法触发快捷指令

---

## 排查步骤

### 步骤 1: 检查背面轻点基础配置

#### 1.1 确认背面轻点已启用

```
设置 → 辅助功能 → 触控 → 背面轻点
```

应该看到：
```
轻点两下: [您的选择或关闭]
轻点三下: [应该显示您的快捷指令名称]
```

**检查项：**
- [ ] 背面轻点菜单存在（不同机型可能不支持）
- [ ] "轻点三下" 有配置内容
- [ ] 配置的是您创建的快捷指令名称

#### 1.2 支持的设备

背面轻点功能仅支持：
```
✅ iPhone 8 及以后机型
✅ iPhone SE (第2代及以后)
❌ iPhone 7 及之前机型不支持
```

---

### 步骤 2: 重新配置背面轻点

很多时候，重新配置可以解决问题：

#### 方法 A: 完整重置

```
1. 打开：设置 → 辅助功能 → 触控 → 背面轻点

2. 轻点三下 → 点击当前选项 → 取消选择
   （设置为"无"或其他选项）

3. 返回上一级，等待 2 秒

4. 再次进入"轻点三下"

5. 向下滚动找到"快捷指令"部分

6. 选择您的快捷指令：
   - 名称：记账助手 OCR（或您命名的名称）
   - 类型：快捷指令

7. 返回，确认显示正确

8. 锁屏，解锁，然后测试
```

#### 方法 B: 先设置轻点两下测试

```
1. 先将快捷指令设置到"轻点两下"
2. 测试是否能触发
3. 如果能触发，说明快捷指令本身没问题
4. 再改回"轻点三下"
```

---

### 步骤 3: 检查快捷指令配置

#### 3.1 快捷指令是否在正确位置

```
打开"快捷指令" App
  ↓
我的快捷指令（底部第一个标签）
  ↓
应该能看到：记账助手 OCR
```

**注意：**
- ❌ 不要放在"文件夹"里
- ❌ 不要在"自动化"标签里
- ✅ 必须在"我的快捷指令"主列表里

#### 3.2 快捷指令内容验证

点击快捷指令右上角的 "..." 编辑，检查：

```
应该包含：
✅ 1. URL: expensetracker://process-screenshot
✅ 2. 打开 URL
```

**常见错误配置：**
```
❌ 只有 "URL" 没有 "打开 URL"
❌ URL 拼写错误（如多了空格）
❌ 使用了 "打开 App" 而不是 "打开 URL"
```

---

### 步骤 4: 系统级检查

#### 4.1 重启 iPhone

这是最有效的方法之一：

```
1. 长按电源键（或电源键+音量键）
2. 滑动关机
3. 等待 10 秒
4. 重新开机
5. 重新测试背面轻点
```

#### 4.2 检查辅助功能权限

```
设置 → 隐私与安全性 → 辅助功能
  ↓
确认"快捷指令" App 有访问权限
```

#### 4.3 检查低电量模式

```
设置 → 电池
  ↓
如果"低电量模式"开启，某些功能可能受限
建议关闭后测试
```

---

### 步骤 5: 快捷指令 App 设置

#### 5.1 允许运行快捷指令

```
设置 → 快捷指令
  ↓
确认以下设置：
✅ 允许运行快捷指令
✅ 允许共享表单快捷指令（如果有）
```

#### 5.2 清除快捷指令缓存

```
1. 打开"快捷指令" App
2. 长按您的快捷指令
3. 选择"复制"
4. 删除原快捷指令
5. 粘贴（创建新副本）
6. 重命名
7. 重新配置背面轻点
```

---

### 步骤 6: 测试方法

#### 正确的测试方式

```
1. 解锁 iPhone（在主屏幕或任意 App 中）
2. 手机竖直拿好
3. 用 2-3 个手指快速轻点手机背面
4. 力度：轻-中等（不要太轻也不要太重）
5. 节奏：ta-ta-ta（快速连续三次）
```

#### 常见测试错误

```
❌ 手机放在桌面上轻点（检测不到）
❌ 轻点太轻（传感器检测不到）
❌ 轻点太重（误判为单次撞击）
❌ 节奏太慢（系统认为是三次独立轻点）
❌ 手机锁屏状态（某些快捷指令可能不工作）
```

---

### 步骤 7: 诊断测试

#### 测试 1: 用系统功能测试背面轻点

```
1. 设置 → 辅助功能 → 触控 → 背面轻点
2. 轻点三下 → 选择"截屏"
3. 返回，轻点背面三次
4. 是否能截屏？
   - ✅ 能：背面轻点硬件/功能正常
   - ❌ 不能：背面轻点功能本身有问题
```

#### 测试 2: 用简单快捷指令测试

创建一个测试快捷指令：

```
1. 新建快捷指令
2. 添加"显示通知"动作
   - 标题：测试
   - 正文：背面轻点成功
3. 保存为"背面轻点测试"
4. 配置到背面轻点
5. 测试是否弹出通知
```

如果测试快捷指令能触发，说明：
- ✅ 背面轻点功能正常
- ❌ 您的 OCR 快捷指令有问题

---

## 已知问题和解决方案

### 问题 1: 背面轻点间歇性失效

**原因：** 系统资源占用高或后台进程卡住

**解决：**
```
1. 关闭后台所有 App
2. 重启 iPhone
3. 不要在充电时测试（充电可能影响检测）
```

### 问题 2: 只有第一次有效，之后失效

**原因：** 快捷指令执行超时或卡住

**解决：**
```
1. 检查应用是否已打开（关闭后重试）
2. 检查快捷指令是否太复杂
3. 简化快捷指令逻辑
```

### 问题 3: 背面轻点选项里找不到快捷指令

**原因：** 快捷指令配置不正确或系统缓存问题

**解决：**
```
1. 确认快捷指令在"我的快捷指令"主列表
2. 重启 iPhone
3. 重新打开设置 App
4. 再次查看背面轻点选项
```

### 问题 4: 配置后显示空白或"未命名"

**原因：** 快捷指令没有正确保存或命名

**解决：**
```
1. 删除该快捷指令
2. 重新创建并**确保命名**
3. 点击"完成"确保保存
4. 重新配置背面轻点
```

---

## 完整重新配置流程

如果以上都不行，从头开始：

### 第 1 步：删除旧配置

```
1. 设置 → 辅助功能 → 触控 → 背面轻点
   → 轻点三下 → 选择"无"

2. 打开"快捷指令" App
   → 删除旧的 OCR 快捷指令

3. 重启 iPhone
```

### 第 2 步：重新创建快捷指令

```
1. 打开"快捷指令" App
2. 点击 "+" 创建新快捷指令
3. 添加 "URL" 动作
   输入：expensetracker://process-screenshot
4. 添加 "打开 URL" 动作
5. 点击顶部名称
   输入：记账助手OCR
   ⚠️ 重要：不要用特殊字符，不要太长
6. 点击"完成"
7. 确认在"我的快捷指令"列表中能看到
```

### 第 3 步：验证快捷指令

```
1. 点击快捷指令运行一次
2. 应该打开 ExpenseTracker 并开始识别
3. 如果不行，检查应用是否安装
```

### 第 4 步：配置背面轻点

```
1. 设置 → 辅助功能 → 触控 → 背面轻点
2. 轻点三下
3. 向下滚动到"快捷指令"
4. 选择"记账助手OCR"
5. 返回，确认显示正确
```

### 第 5 步：测试

```
1. 回到主屏幕
2. 轻点背面三次（ta-ta-ta）
3. 应该触发快捷指令
```

---

## 调试技巧

### 技巧 1: 使用振动反馈

在快捷指令中添加振动，确认是否被触发：

```
快捷指令内容：
1. 振动设备
2. URL: expensetracker://process-screenshot
3. 打开 URL
```

如果轻点背面后：
- ✅ 有振动：快捷指令被触发了
- ❌ 无振动：快捷指令没被触发（背面轻点配置问题）

### 技巧 2: 查看快捷指令历史

```
打开"快捷指令" App
  ↓
点击底部"自动化"标签
  ↓
如果有运行记录，说明快捷指令在执行
```

### 技巧 3: 简化测试

创建最简单的快捷指令：

```
只包含一个动作：
"显示提醒" 
  标题：触发成功
```

如果这个都不能触发，问题在：
- 背面轻点配置
- 系统设置
- 硬件问题

---

## 最终检查清单

请逐项确认：

```
□ iPhone 机型支持背面轻点（iPhone 8+）
□ 快捷指令在"我的快捷指令"主列表
□ 快捷指令有明确的名称
□ 快捷指令包含正确的 URL 和"打开 URL"
□ 快捷指令单独运行正常
□ 背面轻点配置为"轻点三下"
□ 背面轻点选择了正确的快捷指令
□ 已重启 iPhone
□ 测试时手机已解锁
□ 测试时手机竖直握持（不是平放）
□ 轻点力度和节奏适中
```

---

## 联系技术支持

如果以上全部尝试后仍然无法工作：

1. **可能是系统 Bug**
   - 更新到最新 iOS 版本
   - 提交反馈给 Apple

2. **可能是硬件问题**
   - 预约 Apple Store Genius Bar
   - 检查加速度传感器

3. **临时替代方案**
   - 使用主屏幕小组件
   - 使用 Siri 语音触发
   - 手动打开应用


