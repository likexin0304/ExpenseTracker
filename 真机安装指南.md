# 真机安装指南

## 前提条件

- ✅ Xcode 已安装
- ✅ iPhone 通过 USB 连接到 Mac
- ✅ iPhone 已解锁
- ✅ 信任此电脑（如果是首次连接）

---

## 步骤 1: 连接 iPhone

1. **使用 USB 线连接 iPhone 到 Mac**

2. **iPhone 上可能会弹出提示**：
   ```
   "信任此电脑？"
   → 点击"信任"
   → 输入 iPhone 密码
   ```

3. **在 Xcode 顶部工具栏检查设备**：
   ```
   应该显示：Your iPhone Name (iOS 版本)
   ```

---

## 步骤 2: 配置签名（重要）

### 如果没有付费开发者账号

1. **打开 Xcode**

2. **打开项目**：`ExpenseTracker.xcodeproj`

3. **选择项目根节点** → **TARGETS** → **ExpenseTracker**

4. **Signing & Capabilities** 标签页

5. **配置签名**：
   ```
   ✅ 勾选 "Automatically manage signing"
   
   Team: 
   如果没有看到选项，点击 "Add an Account..."
     └─ 输入您的 Apple ID
     └─ 登录
     └─ 返回后选择您的 Apple ID (Personal Team)
   
   Bundle Identifier: 
   应该显示 com.kexin.ExpenseTracker
   （如果冲突，改成 com.yourname.ExpenseTracker）
   ```

6. **可能会看到警告**：
   ```
   ⚠️ "Signing for "ExpenseTracker" requires a development team."
   → 选择您的 Apple ID Team 即可解决
   ```

---

## 步骤 3: 安装应用

### 方法 A: 通过 Xcode 运行（推荐）

1. **确认设备已选中**：
   ```
   Xcode 顶部：ExpenseTracker > Your iPhone Name
   ```

2. **点击运行按钮**：
   ```
   ▶️ 按钮 或 按 ⌘R
   ```

3. **首次安装时，iPhone 上会弹出提示**：
   ```
   "不受信任的开发者"
   → 先点"取消"
   → 打开 设置 → 通用 → VPN与设备管理
   → 点击您的 Apple ID
   → 点击"信任 [您的 Apple ID]"
   → 返回主屏幕
   → 再次从 Xcode 运行 ⌘R
   ```

4. **等待编译和安装**：
   ```
   Xcode 顶部会显示进度：
   "Building..." → "Running..." → "Running ExpenseTracker on iPhone"
   ```

5. **应用自动打开**

### 方法 B: 手动安装（不运行）

如果只想安装不运行：

```
⌘B (只编译)
然后从 Xcode 菜单：
Product → Run Without Building (⌘⌃R)
```

---

## 步骤 4: 验证安装

### 在 iPhone 上检查

1. **主屏幕应该有 ExpenseTracker 图标**

2. **打开应用**：
   - 应该能正常登录
   - 主界面正常显示

3. **检查设置**：
   ```
   设置 → 通用 → VPN与设备管理
   应该看到您的 Apple ID 和 ExpenseTracker
   ```

### 验证 URL Scheme

在 Safari 中测试：

1. **打开 Safari**

2. **在地址栏输入**：
   ```
   expensetracker://process-screenshot
   ```

3. **回车，应该：**
   - ✅ 弹出提示："在 ExpenseTracker 中打开？"
   - ✅ 点击"打开"后，应用启动并开始识别流程

**如果没有弹出提示或提示"找不到应用"**：
- → URL Scheme 没有正确注册
- → 需要重新安装应用

---

## 步骤 5: 重新配置快捷指令

应用安装成功后：

1. **打开"快捷指令" App**

2. **找到之前创建的快捷指令**：
   - 如果已有，删除重建（避免缓存问题）

3. **重新创建**：
   ```
   1. URL: expensetracker://process-screenshot
   2. 打开 URL
   ```

4. **测试快捷指令**：
   - 点击快捷指令运行
   - 应该打开应用并开始识别

5. **重新配置背面轻点**：
   ```
   设置 → 辅助功能 → 触控 → 背面轻点
   → 轻点三下 → 选择快捷指令
   ```

---

## 常见问题

### Q1: Xcode 找不到我的 iPhone？

**解决方法**：
```
1. 检查 USB 连接
2. 重启 iPhone
3. 重启 Xcode
4. 在 iPhone 上再次"信任此电脑"
5. Xcode → Window → Devices and Simulators
   → 检查设备是否显示
```

### Q2: 签名错误 "Failed to register bundle identifier"

**解决方法**：
```
Bundle ID 已被占用，修改为唯一值：
com.kexin.ExpenseTracker
→ 改为
com.[你的名字].ExpenseTracker
或
dev.kexin.ExpenseTracker
```

### Q3: "不受信任的开发者" 错误

**解决方法**：
```
设置 → 通用 → VPN与设备管理
→ 点击您的 Apple ID
→ 信任
```

### Q4: 编译成功但安装失败

**解决方法**：
```
1. 清理构建：⇧⌘K (Shift + Command + K)
2. 删除 DerivedData：
   Xcode → Preferences → Locations
   → 点击 DerivedData 路径的箭头
   → 删除 ExpenseTracker 相关文件夹
3. 重新编译：⌘B
4. 重新运行：⌘R
```

### Q5: 应用安装后闪退

**检查项**：
```
1. 查看 Xcode Console 的错误日志
2. 检查 Info.plist 配置是否正确
3. 检查权限设置（屏幕录制等）
```

---

## 完整测试流程

安装成功后，完整测试：

```
✅ 1. 应用可以正常打开
✅ 2. 可以正常登录
✅ 3. 主界面正常显示
✅ 4. Safari 测试 URL Scheme 成功
✅ 5. 快捷指令可以打开应用
✅ 6. 背面轻点可以触发快捷指令
✅ 7. 完整 OCR 识别流程正常工作
```

---

## 开发提示

### 免费 Apple ID 限制

使用免费 Apple ID（Personal Team）的限制：
```
⚠️ 每 7 天需要重新安装一次
⚠️ 只能安装 3 个应用
⚠️ 部分高级功能不可用（如推送通知）
```

### 付费开发者账号优势

如果需要长期使用：
```
✅ 无需每周重新安装
✅ 可以分发给其他设备
✅ 支持所有功能
✅ 可以上架 App Store

费用: $99/年
```

---

## 快速参考

### Xcode 快捷键

```
⌘R    - 运行 (Run)
⌘B    - 编译 (Build)
⇧⌘K   - 清理 (Clean)
⌘.    - 停止运行 (Stop)
⌘0    - 显示/隐藏导航栏
⌘⇧Y   - 显示/隐藏控制台
```

### 设备位置

```
Xcode 顶部：
[Scheme 选择器] > [设备选择器]
  
点击设备选择器：
- iPhone 模拟器列表（用于开发测试）
- 连接的真机（需要 USB 连接）
```

---

## 总结

完成以上步骤后：
1. ✅ 应用已安装到 iPhone（Bundle ID: com.kexin.ExpenseTracker）
2. ✅ URL Scheme 已注册（expensetracker://）
3. ✅ 快捷指令可以正常工作
4. ✅ 背面轻点可以触发自动识别


