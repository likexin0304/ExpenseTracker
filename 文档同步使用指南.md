# 📚 文档同步使用指南

本指南介绍如何在另一个项目中引用ExpenseTracker的文档。

---

## 🎯 场景选择

### 场景1：本地开发，需要实时同步
**推荐方案**: 符号链接（Symbolic Link）

### 场景2：团队协作，多人使用
**推荐方案**: Git子模块（Submodule）

### 场景3：定期手动同步
**推荐方案**: 使用同步脚本

### 场景4：自动监控变化
**推荐方案**: 使用watch脚本

---

## 方法1：符号链接（最简单）

### 使用步骤

```bash
# 1. 进入后端项目
cd /path/to/your/backend-project

# 2. 创建docs目录（如果不存在）
mkdir -p docs

# 3. 创建符号链接
ln -s /Users/kexin.li/Desktop/ExpenseTracker/后端API需求清单.md docs/后端API需求清单.md

# 4. 验证
ls -la docs/后端API需求清单.md
cat docs/后端API需求清单.md
```

### 优点
- ✅ 最简单，一行命令搞定
- ✅ 源文件更新后，自动同步
- ✅ 不占用额外空间

### 缺点
- ❌ 不能提交到Git（路径是绝对路径）
- ❌ 只能在同一台机器上工作
- ❌ 其他团队成员无法使用

### 适用场景
- 只有你一个人本地开发
- 不需要提交到Git
- 两个项目在同一台机器上

---

## 方法2：手动同步脚本（推荐）

### 使用步骤

```bash
# 1. 修改sync-docs.sh中的TARGET_PROJECT路径
# 将 /Users/kexin.li/Desktop/Backend-Project 改为你的实际路径

# 2. 赋予执行权限
chmod +x sync-docs.sh

# 3. 运行同步
./sync-docs.sh

# 输出示例：
# 🔄 开始同步文档...
# ✅ 已同步: 后端API需求清单.md
# ✅ 已同步: API-Backend.md
# 🎉 同步完成！
```

### 使用配置文件

```bash
# 1. 修改.sync-config.json中的路径
# "path": "/Users/kexin.li/Desktop/Backend-Project"

# 2. 运行（如果实现了配置支持）
./sync-docs.sh --config .sync-config.json
```

### 优点
- ✅ 可以提交到Git
- ✅ 团队成员都能用
- ✅ 可以同步到多个项目
- ✅ 可以选择性同步文件

### 缺点
- ❌ 需要手动运行
- ❌ 可能会忘记同步

### 适用场景
- 多人团队协作
- 需要提交到Git
- 需要同步到多个项目

---

## 方法3：自动监控同步（高级）

### 使用步骤

```bash
# 1. 赋予执行权限
chmod +x watch-and-sync.sh

# 2. 启动监控（在后台运行）
nohup ./watch-and-sync.sh /path/to/backend-project > sync.log 2>&1 &

# 3. 查看日志
tail -f sync.log

# 4. 停止监控
ps aux | grep watch-and-sync
kill <进程ID>
```

### 优点
- ✅ 完全自动化
- ✅ 实时同步
- ✅ 不需要记住手动同步

### 缺点
- ❌ 需要持续运行
- ❌ 占用一个后台进程

### 适用场景
- 频繁修改文档
- 需要实时同步
- 长期开发

---

## 方法4：Git子模块（团队推荐）

### 使用步骤

```bash
# 1. 确保ExpenseTracker已推送到Git
cd /Users/kexin.li/Desktop/ExpenseTracker
git add 后端API需求清单.md .sync-config.json
git commit -m "添加文档同步配置"
git push origin main

# 2. 在后端项目中添加子模块
cd /path/to/backend-project
git submodule add <ExpenseTracker-Git-URL> vendor/expense-tracker-docs

# 3. 初始化子模块
git submodule update --init --recursive

# 4. 创建软链接到docs目录
mkdir -p docs
ln -s ../vendor/expense-tracker-docs/后端API需求清单.md docs/后端API需求清单.md

# 5. 提交
git add .gitmodules vendor/ docs/
git commit -m "添加前端文档子模块"
git push
```

### 更新子模块

```bash
# 拉取最新的文档
cd /path/to/backend-project
git submodule update --remote vendor/expense-tracker-docs

# 提交更新
git add vendor/expense-tracker-docs
git commit -m "更新前端文档"
git push
```

### 团队成员使用

```bash
# 克隆项目时
git clone --recursive <backend-project-url>

# 或者克隆后初始化子模块
git clone <backend-project-url>
cd backend-project
git submodule update --init --recursive
```

### 优点
- ✅ 正规的Git解决方案
- ✅ 团队所有人都能用
- ✅ 版本可控
- ✅ 可以锁定特定版本

### 缺点
- ❌ 需要两个项目都在Git上
- ❌ 配置相对复杂
- ❌ 会引入整个项目（不只是一个文件）

### 适用场景
- 正式的团队项目
- 需要版本控制
- 多人协作开发

---

## 🎯 快速决策表

| 需求 | 推荐方案 | 命令 |
|------|---------|------|
| 只有我自己用 | 符号链接 | `ln -s ...` |
| 团队协作，可以手动同步 | 同步脚本 | `./sync-docs.sh` |
| 需要实时自动同步 | 监控脚本 | `./watch-and-sync.sh` |
| 正式项目，需要版本控制 | Git子模块 | `git submodule add` |

---

## 📝 实战示例

### 示例1：后端项目需要API文档

```bash
# 方式A：符号链接（最快）
cd ~/Projects/backend-api
mkdir -p docs
ln -s ~/Desktop/ExpenseTracker/后端API需求清单.md docs/

# 方式B：复制一次（最简单）
cd ~/Projects/backend-api
mkdir -p docs
cp ~/Desktop/ExpenseTracker/后端API需求清单.md docs/
```

### 示例2：多个项目需要同步

```bash
# 1. 修改sync-docs.sh，添加多个目标项目
TARGET_PROJECTS=(
    "~/Projects/backend-api"
    "~/Projects/backend-docs"
    "~/Projects/api-gateway"
)

# 2. 运行同步
./sync-docs.sh
```

### 示例3：自动化同步（推荐开发期间使用）

```bash
# 在ExpenseTracker项目中
chmod +x watch-and-sync.sh

# 启动自动同步到后端项目
./watch-and-sync.sh ~/Projects/backend-api

# 现在每次修改后端API需求清单.md，都会自动同步！
```

---

## 🔧 配置说明

### sync-docs.sh 配置

修改脚本开头的配置部分：

```bash
# ==================== 配置 ====================
SOURCE_PROJECT="/Users/kexin.li/Desktop/ExpenseTracker"
TARGET_PROJECT="/path/to/your/backend-project"  # 改这里

# 要同步的文件列表
FILES=(
    "后端API需求清单.md"
    "API-Backend.md"
    # 可以添加更多文件
)
```

### .sync-config.json 配置

```json
{
  "targetProjects": [
    {
      "name": "后端项目",
      "path": "/实际/路径/to/backend-project",  // 改这里
      "enabled": true  // 设为true启用
    }
  ]
}
```

---

## ❓ 常见问题

### Q1: 符号链接在Windows上能用吗？
A: 可以，但需要管理员权限：
```cmd
mklink "C:\path\to\backend\docs\后端API需求清单.md" "C:\path\to\ExpenseTracker\后端API需求清单.md"
```

### Q2: 能不能在云端同步？
A: 可以！如果两个项目都在iCloud/Dropbox/OneDrive等云盘中，符号链接会自动同步。

### Q3: 同步脚本能自动化吗？
A: 可以！添加到crontab定时执行：
```bash
# 每小时同步一次
0 * * * * /path/to/ExpenseTracker/sync-docs.sh
```

### Q4: 如何只同步特定文件的一部分？
A: 可以在另一个项目的README中链接到这个文档的特定章节：
```markdown
详见：[OCR确认API](../docs/后端API需求清单.md#ocr确认并创建支出api)
```

---

## 🎉 推荐工作流

### 日常开发（推荐）

```bash
# 1. 使用符号链接（开发时）
ln -s ~/Desktop/ExpenseTracker/后端API需求清单.md ~/Projects/backend/docs/

# 2. 修改ExpenseTracker中的文档
# 后端项目自动看到更新

# 3. 提交前，运行同步脚本复制文件
./sync-docs.sh
cd ~/Projects/backend
git add docs/
git commit -m "更新API文档"
```

### 团队协作（推荐）

```bash
# 使用Git子模块 + 同步脚本
# 1. 设置子模块（一次性）
git submodule add <url> vendor/docs

# 2. 定期更新
git submodule update --remote

# 3. 复制到docs目录（便于查看）
./sync-docs.sh
```

---

## 📞 需要帮助？

如果遇到问题：
1. 检查路径是否正确
2. 检查文件权限（`ls -la`）
3. 查看sync.log日志
4. 确保目标项目存在

---

**最后更新**: 2025-01-17  
**维护者**: ExpenseTracker iOS Team

