# 快捷指令配置指南

## 方案一：URL Scheme 方式（推荐）

### 创建快捷指令

1. **打开"快捷指令" App**

2. **点击右上角 "+" 创建新快捷指令**

3. **添加动作 - URL**
   - 在搜索框输入 "URL"
   - 选择 "URL" 动作
   - 输入：`expensetracker://process-screenshot`

4. **添加动作 - 打开 URL**
   - 在搜索框输入 "打开"
   - 选择 "打开 URL" 动作
   - 这个动作会自动使用上一步的 URL

5. **命名快捷指令**
   - 点击顶部快捷指令名称
   - 改名为：`记账助手 OCR` 或 `账单识别`

6. **点击"完成"保存**

### 配置背面轻点

1. **打开"设置" App**

2. **辅助功能** → **触控**

3. **背面轻点**

4. **选择"轻点两下"或"轻点三下"**
   - 建议使用"轻点三下"（避免误触）

5. **在列表中找到并选择刚才创建的快捷指令**
   - 向下滚动到"快捷指令"部分
   - 选择 `记账助手 OCR` 或您命名的快捷指令

6. **完成配置**

### 测试流程

1. **确保应用已安装并可以正常打开**

2. **打开任意账单页面**（如微信支付成功页面）

3. **轻点手机背面三次**

4. **预期行为**：
   - 应用自动打开
   - 开始截图和 OCR 识别
   - 显示识别进度
   - 如果识别成功且置信度高，自动创建支出
   - 如果识别失败或置信度低，显示确认界面

---

## 方案二：直接打开应用方式

### 创建快捷指令

1. **打开"快捷指令" App**

2. **点击右上角 "+" 创建新快捷指令**

3. **添加动作 - 打开 App**
   - 在搜索框输入 "打开"
   - 选择 "打开 App" 动作
   - 在"App"字段中选择 `ExpenseTracker`

4. **命名快捷指令**
   - 改名为：`打开记账助手`

5. **点击"完成"保存**

### 注意事项

⚠️ **方案二的限制**：
- 只会打开应用，**不会自动触发 OCR 识别**
- 需要手动点击"自动识别"按钮
- **不推荐用于背面轻点自动化**

---

## 方案三：增强版（URL + 延迟）

如果方案一有时候不稳定，可以添加延迟：

### 创建快捷指令

1. **打开"快捷指令" App**

2. **点击右上角 "+" 创建新快捷指令**

3. **添加动作 - URL**
   - 输入：`expensetracker://process-screenshot`

4. **添加动作 - 等待**
   - 在搜索框输入 "等待"
   - 选择 "等待" 动作
   - 设置：`0.5` 秒

5. **添加动作 - 打开 URL**
   - 选择 "打开 URL" 动作

6. **命名并保存**

---

## 故障排查

### 问题 1：轻点背面没有反应

**解决方法**：
1. 确认背面轻点已启用
2. 确认选择了正确的快捷指令
3. 尝试重启 iPhone
4. 重新配置背面轻点

### 问题 2：应用打开但没有开始识别

**检查项**：
1. 确认使用的是方案一（URL Scheme）
2. 检查 URL 是否正确：`expensetracker://process-screenshot`
3. 查看应用日志中是否有"收到URL回调"的提示

### 问题 3：快捷指令找不到应用

**解决方法**：
1. 确认应用已安装（Bundle ID: `com.kexin.ExpenseTracker`）
2. 尝试重启 iPhone
3. 重新从 Xcode 安装应用

### 问题 4：权限提示

**首次运行时需要授权**：
1. 屏幕录制权限（用于截图）
2. 通知权限（用于提示识别结果）

在设置中手动授权：
- **设置** → **隐私与安全性** → **屏幕录制** → 启用 ExpenseTracker
- **设置** → **通知** → ExpenseTracker → 允许通知

---

## 推荐配置

### 最佳实践

```
✅ 使用方案一（URL Scheme）
✅ 配置"轻点三下"（避免误触）
✅ 提前授予屏幕录制权限
✅ 在账单页面测试（如微信支付成功页面）
```

### 完整工作流程

```
1. 看到账单（如微信支付成功）
2. 轻点手机背面三次
3. 自动打开记账助手
4. 自动截图 + OCR 识别
5. 自动解析账单信息
6. 高置信度：自动创建支出记录
7. 低置信度：显示确认界面供手动修改
```

---

## 高级技巧

### 添加提示音

可以在快捷指令中添加提示音，让操作更有反馈感：

1. 在 URL 动作后添加 "播放声音" 动作
2. 选择喜欢的提示音

### 添加振动反馈

1. 添加 "振动设备" 动作
2. 选择振动类型

### 示例流程（完整版）

```
1. URL: expensetracker://process-screenshot
2. 振动设备
3. 等待: 0.5 秒
4. 打开 URL
5. 播放声音: "提示"
```

---

## 测试清单

配置完成后，按以下清单测试：

- [ ] 轻点背面三次，应用是否自动打开
- [ ] 应用打开后是否显示"开始识别"进度
- [ ] 是否自动截图
- [ ] 是否开始 OCR 识别
- [ ] 识别成功后是否显示结果或确认界面
- [ ] 如果识别失败，是否显示手动输入界面

---

## 常见场景测试

### 场景 1：微信支付账单
1. 打开微信支付成功页面
2. 轻点背面三次
3. 检查是否正确识别金额、商户、时间

### 场景 2：支付宝账单
1. 打开支付宝付款成功页面
2. 轻点背面三次
3. 检查识别准确度

### 场景 3：京东订单
1. 打开京东支付成功页面
2. 轻点背面三次
3. 检查是否正确识别

### 场景 4：非账单页面
1. 打开任意非账单页面（如设置页面）
2. 轻点背面三次
3. 预期：显示确认界面，让用户手动输入

---

## 联系与支持

如果遇到问题：
1. 检查应用日志（在 Xcode Console 中查看）
2. 确认 Bundle ID: `com.kexin.ExpenseTracker`
3. 确认 URL Scheme: `expensetracker://process-screenshot`


